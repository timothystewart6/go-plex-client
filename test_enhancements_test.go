package plex

import (
	"testing"
)

// Test the functions in test_enhancements.go to bring them to 80%+ coverage
func TestTestFlexibleIntUnmarshaling(t *testing.T) {
	// This function doesn't return anything, just tests that it doesn't panic
	TestFlexibleIntUnmarshaling(t)
	// Since it's a test function, we just need to call it
}

func TestTestSubtitleDecisionInTranscodeSession(t *testing.T) {
	// This function doesn't return anything, just tests that it doesn't panic
	TestSubtitleDecisionInTranscodeSession(t)
	// Since it's a test function, we just need to call it
}

func TestTestTimelineEventHandler(t *testing.T) {
	// This function doesn't return anything, just tests that it doesn't panic
	TestTimelineEventHandler(t)
	// Since it's a test function, we just need to call it
}
